name: medical_app
description: Medical Records Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase - إصدارات قديمة متوافقة
  firebase_core: ^2.24.2
  cloud_firestore: ^4.13.6
  firebase_storage: ^11.5.6
  firebase_auth: ^4.15.3

  # باقي الحزم
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  image_picker: ^1.1.2
  pdf: ^3.10.8
  printing: ^5.12.0
  path_provider: ^2.1.3
  google_sign_in: ^6.2.1
  googleapis: ^13.2.0
  fluttertoast: ^8.2.4
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
